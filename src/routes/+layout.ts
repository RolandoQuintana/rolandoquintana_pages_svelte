// This sets static prerendering
export const prerender = true;

// This disables server-side rendering completely
export const ssr = false;

// No trailing slash in routes
export const trailingSlash = "never";
